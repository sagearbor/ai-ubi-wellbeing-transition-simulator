# Uploaded to Cloud Build for `gcloud run deploy --source .`
# .env.local is deliberately NOT excluded: Vite reads GEMINI_API_KEY from it at build time.
.git
.gitignore
node_modules
dist
tmp
examples
# docs/ and *.md stay in: the Model Card tab imports docs/design/model-card-default.md at build time.
.claude
.env
App.tsx.backup
