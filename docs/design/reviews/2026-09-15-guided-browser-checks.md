# Guided interface: browser acceptance, 15 September 2026

The coordinator exercised real Chrome controls in isolated sessions. This is automated interaction and layout evidence, not a first-time-user study. No provider key or AI-backed extraction was used. Evidence files are in [guided-2026-09-15](evidence/guided-2026-09-15/).

## Exact builds

- Corrected interface: `ab5216853f4d926d0caba47b0f9b78ddd4e765d5`, served on local port 4183. Entry `index-9E6-EEsE.js`, SHA256 `aa2b2b45369207810d38cf496bead8f0500dc42d3464a9839d21569e201ed57e`.
- After main and review-document carry-forward, `npm run check` passed at `4a212a9e722fb3ca857c20e1701d74b55aabbdf9`: **75 files, 1,086 tests**, all required validators, source gate, live ledger and production build. Its entry bytes match the corrected preview exactly. The final changes since that head are reporting artifacts only.
- Matched original interface: `eb80fbf6fb19088e6188698646933e16aa82b5e0`, port 4180, entry `index-D3WWcq0i.js`, SHA256 `2a884b419ffdc359892fb7a5b72465701df1fed85e560a3703a19b9ffa91d2f3`. The main commits after this head change only documentation; both interfaces use the same reviewed economic code/data. The UI diff against main has no changes in simulation, core/policy/worker engines, data or dependencies.
- Initial UI accounting and cancellation probes below used `3f09bb5`, port 4182, entry `index-Da049lUr.js`, SHA256 `947aa0df9420adf72bab13b733c988a3cf740edc41885b35202e3acef1058b5d`. These are not evidence for the later corrected navigation code. The fixes do not change the allocator or worker execution.

## Corrected interface: observed outcomes

| Flow | Actual result |
|---|---|
| Compare → Compare world maps → Exit map comparison | Single map restored; comparison-only exit/sidebar disappears; main month-zero accounting remains visible. |
| Select Organic Incentive legacy model → home | Model preserved; actual legacy-flow scope displayed; conditional restart explicitly says it opens month zero and removes uploaded world equations. Save-current action is visible. |
| Start conditional dividend reference | Real default initialization, funded-flow view and reviewed default accounting restored. This is explicit scenario replacement, not ordinary navigation. |
| Explore AI risks → Explore | Separate Futures model opens; return restores world funding and accounting context. |
| Try a policy idea → S.3877 example → Run paired comparison | Source has 16,182 characters; 1/23 listed provisions mapped, 10 unresolved and 12 outside-model. Quotation accounting covers 79/79 clauses; operative dispositions still have 68 unresolved. No completeness attestation. |
| Policy → native Charts | Draft A identity `b64371b1d188b4ef`, annual 2026–2029 calendar, training-model scope, partial-coverage warning. Source textarea remains mounted with all 16,182 characters. |
| Policy → Explore | Explicit active-policy panel with resume/edit/world-switch actions. No unrelated world funding controls or invented geographical results. |
| Add B → Charts without running | Draft B is empty; no current successful calculation displayed. Raw probe's `hasATable:true` counts the hidden retained Lab table, not a visible A result. |
| Edit B's mapped addition from +40,000,000 to −40,000,000 → run | Deliberately adversarial mapping fails `nonnegative-final-values` at 2026. Charts says draft B failed, exposes invariant details, displays no current successful calculation and has **zero visible tables**. This is a failure-handling probe, not a proposed reading of the bill. |
| Share at 320px under reduced-motion preference | No page overflow. Focus enters the close button; Escape closes the modal and returns focus to Share this scenario. |
| Fresh Explore, light and dark | axe-core 4.12.1: 0 violations, 0 incomplete checks on each tested page. No browser errors in this session. This does not certify every tab or assistive-technology interaction. |
| 1440×900, 1280×720, 390×844 and 320×844 | Screenshots visually inspected; 390px and 320px document-width probes show no horizontal overflow. Mobile receipt preview is visible before the long form. Both color themes inspected; reduced-motion preference enabled for the final keyboard probe. |

The branch's 8 App transition tests independently exercise preservation of edited paired runs, uploaded-hook clearing on explicit restart, and comparison guard behavior. They invoke actual App handlers with a hook host, not a browser DOM. The [scoped independent re-review](2026-09-15-guided-interface-rereview.md) records that limitation.

## Allocator and worker observations on the initial UI

Actual Apple amount request 40 billion/month, headquarters-country route: funded 24.375 billion, unfunded 15.625 billion, 340 million eligible residents. Set available share to 25%: funded 6.09375, reserved 18.28125, unfunded 33.90625 billion/month. Set request to zero: receipts/funding zero, unused 6.09375 and reserved 18.28125; eligibility remains 340 million residents. Independent reviewer also hand-checked below/at/above-source cases against a constructed two-country scenario.

Actual Add a variable entry focuses the authoring section, offers `potential_placements` as the training-model hook and applies `newAccess` with multiplier `1 + 0.05 * newAccess`; final invariant constraints remain downstream. Root did not re-certify arbitrary model creation/export combinations. Existing core sharing/replay evidence is linked below.

For a genuine cancellation event, the coordinator imported an explicitly artificial 1,000-step load-test model and enabled uncertainty. A MutationObserver clicked the **actual rendered Cancel button** as soon as it appeared; no worker/model state was mocked or directly changed. UI reported `Uncertainty cancelled: Cancelled.` and no running job. The automation handles a fast button; this is not a human timing/usability measurement.

A preceding 4,000-step version never ran uncertainty: its conservative 9,936,055 retained cells exceeded the 4,000,000-cell budget. The timed-out Cancel lookup on that first attempt is **limit-refusal evidence, not cancellation evidence**. It is not counted as a cancellation pass.

## Sharing, remaining limitations and reproduction

[Core browser evidence](2026-09-15-stage35-browser-checks.md) records actual complete-bundle download/reopen with all 288 numerical values identical; a compact B link; long-link refusal; world corporate-edit reopen; valid/invalid owner hashes and native policy Charts. Guided routing preserves this reviewed core and has explicit link-owner tests. These flows were not all duplicated on the cosmetic integration head. Arbitrary messaging-channel delivery of the very long world link, live Gemini extraction, Safari/Firefox and real user comprehension remain **not verified**.

Public-site asset identifiers checked at 2026-09-15T11:22:33Z match the earlier public identifiers, not either local review build. Deployed commit is **not verified**. No cloud deployment was performed.

The old entry screen's global-only pool dividend and the new selected-Apple per-recipient figure are intentionally different quantities. Their values must not be presented as an economic before/after change. Both screenshots use the same starting world state; their emphasis differs.

Known follow-ups: advanced Lab and legacy detail views remain dense; main entry JavaScript is about 1.81 MB minified (0.48 MB gzip); no slow-network field study. The old external Google Fonts link remains in the shared HTML shell, although the guided theme uses local fonts. A deliberately corrupt conditional snapshot missing its output definition can fall through to an anchored label; no supported load/navigation route reproduced it, so the reviewer recorded this as defensive hardening rather than a release blocker. No empirical or human approval is implied by the accounting badge.
