# Forensic Analysis: beta delivery final integration

**Domain Lens:** Principal engineer reviewing scientific-software provenance, React state, accessibility semantics, and release-evidence boundaries.

**Reviewed range:** `cb29ab2..e6a02a8`; task 6 specifically `e399b4f..941c8c4`. Reviewed on 2026-09-15. The three subsequently supplied audit artifacts are documentary evidence only; their context hash matches the current lockfile. No production files were changed by this reviewer.

## Executive Summary

**REQUEST ONE IMPORTANT INTEGRATION FIX before acceptance. Task 6 is accepted within its selector-accessibility scope.** The exact financial authoring handoff exposes a form that cannot be completed, without explaining its limitation. No critical defect was found. Any subsequent acceptance will cover the bounded integration, not deployment, successful candidate AI extraction, scientific forecasting validity, publication eligibility, or a comprehensive accessibility/security certification.

The prior continuity, financial-source and security/qualification reviews remain the scoped authorities; their completed sequences were not restarted. Current integration preserves exact financial identities, immutable observations and frozen results, the strict runtime refusal, and acceptance of only 61 baseline accounting states. The external delivery gates remain explicitly unfinished.

## Claim Verification

| Claim | Verdict | Evidence / reasoning |
|---|---|---|
| Review package represents the requested range | Verified | Programmatically compared all 79 file sections with `git diff cb29ab2..e6a02a8`. Added/deleted lines and object identities match exactly. Package header and expanded context explain its different total size. Large JSON was compared structurally, not read as a prose diff. |
| Financial A/B and collection identity survive the combined changes | Verified, scoped | Independent Node 22 probe exercised 24 current-runtime handoffs: both collections, all six companies, both sides, unequal edited shares, URL encode/decode, policy destination and app-built origin. All matched exact scenarios and collections. Removing origin prevents that label; an old numerical hash is refused. |
| Trust labels do not acquire scientific authority | Verified | `importFinancialExperiment` validates pinned inputs; `verifiedFinancialOrigin` rechecks catalog/model/source identity. Lab describes these as illustrative allocation assumptions. Prior adversarial import review remains applicable; no subsequent production change weakens its boundary. |
| V1 and historical evidence are preserved | Verified | V1 observations retain SHA-256 `fa0017ef898082cb7289448a2fc7e174a39039c8b67bd9683aacc8ea7bc64dba`; original captures retain `12bb8f688e1e087aeda1bfa6d3edceb08b9924c42c6ac818690c890d8c909d6b`. Recursive comparison of both experience records finds only `/sourceHashes/package-lock.json` changed. Frozen witnesses are covered by the prior independent security review; no later changes alter them. |
| Qualification remains narrowly bound | Verified | Fresh verification matched all 33 source-file hashes, structure/payload references, and exactly 61 unique authority identities equal to the manifest baseline array. Raw evidence remains present at the declared 504,339,058 bytes. This review did not repeat the earlier full 383-case raw-stream checksum and arithmetic audit. |
| Security patch is compatible without falsifying replay | Verified, bounded | Actual pre/post browser financial captures differ only in numericalHash. Explicit refusal preserves the current financial experiment. Old runtime is not bundled, and no engine/bundle gate was weakened. |
| Audit summary is honest | Verified as recorded evidence | Supplied production audit reports zero vulnerabilities; full audit reports two moderate package flags. Its recorded lock SHA-256 matches the current file. Prior review identifies one development-only Vitest advisory and bounds its exposure. No fresh network audit or comprehensive security certification is claimed here. |
| Software checks passed | Verified as retained root-run evidence | Inspected final log: 1,257 tests / 90 files and completed build. Known AT-3 failure and ledger limits remain visible. This reviewer did not duplicate the full suite; independently ran ten PolicyPanel lifecycle tests, all passing on Node 26.8.2. The financial integration probe separately passed on Node 22.23.2. |
| MIT/citation/release documents support a prepared handoff | Verified, bounded | MIT text and contributor-group citation exist; metadata makes no assigned DOI claim. Third-party notices explicitly exclude third-party rights from the software grant and defer distribution-specific review. Deployment procedure matches the existing no-traffic tagged deployment script. Expert contact, publication and promotion remain prepared actions. This is not a legal-rights inventory or independent confirmation of journal eligibility. |
| Candidate AI is verified | Not claimed; unresolved | Actual recorded provider rejection is retained and labeled unverified. Older public-site success does not satisfy the candidate gate. Browser-key architecture is the owner's accepted scope; no gateway change is inferred. |

## Internal Consistency Issues

**Important — the financial “Add a variable” handoff is an undisclosed dead end.** `PublishedExperience.tsx:593` now sends exact financial scenario A to the authoring form. Every financial variable has `hook: false` (`src/financials/model.ts:59`). `AddVariableForm.tsx:133` therefore renders an empty Target variable selector but retains its enabled Apply button. `emptyOverlayForm` selects an empty target, and `applyOverlayForm` rejects it with “Effect 1 needs a target variable.” The root browser observation and an independent Node 22 probe both support this defect. The surrounding help promises direct equation attachment without explaining that this model cannot accept any such attachment. This is a newly exposed integration failure despite the underlying form predating this change.

**Minimal fix:** leave financial equations, hooks and identities unchanged. When no eligible targets exist, present an explicit unavailable explanation and concrete model-file export/edit/import route; hide or disable the unusable creation form. Adjust the published CTA/copy so it offers inspection of extension options rather than promising direct addition. Preserve existing overlay removal and working authoring for hook-enabled models. Verify the actual financial route and one supported authoring model.

Otherwise, no blocking contradiction found. The financial-source review predates the dependency patch, so its original replay acceptance is correctly superseded by explicit runtime incompatibility. Newly constructed experiments may still use retained v1 observations; old saved experiments are not silently repinned. The evidence manifest's generation-time pending review label and the later active authority are distinguished by the scoped activation addendum.

Release documents accurately distinguish code prepared on the branch from what is live. Their mention of a “planned security patch” is stale future-tense wording, but the same paragraph states the required old-runtime refusal and the current readiness report documents the completed patch. This is optional editorial cleanup, not a release-evidence or implementation blocker.

## Task 6: selector accessibility verdict

**ACCEPT.** `PolicyPanel.tsx:636` now uses a named `group` containing native `button` elements with `aria-pressed`. Exactly one draft is pressed; Add/Remove is outside the group. The buttons retain `type="button"`, ordinary tab order and native Enter/Space activation. Tablist arrow-key behavior is no longer falsely promised. This is a valid mutually exclusive pressed-button group; it does not require tabpanel semantics.

Diff inspection confirms selection still calls only `setActive(i)`. Drafts, edits, result arrays and Add/Remove handlers retain their prior behavior; the repair adds no state-reset path or custom keyboard interception. The independently rerun lifecycle suite passes all ten tests, including the new selection-state regression.

The actual browser before artifact records the critical `aria-required-children` failure; the after artifact records zero violations and zero incomplete checks. The retained keyboard artifact shows B focused/pressed, A unpressed, and no draft action inside the group. The root browser record supplies the Tab/Enter/Space execution and repeated 288-value bundle reproduction. These artifacts support the bounded verdict; they are not a screen-reader listening session or whole-product accessibility audit. History's separate incomplete contrast check remains disclosed.

## Best Practices Violations

The enabled but impossible financial authoring flow violates the interface’s capability contract; resolve the important issue above. The residual Vitest advisory remains an explicitly documented follow-up rather than an audit-clean claim. Broad causal or human-review authority is not inferred from automated testing or this automated review.

## Unaddressed Failure Modes

Successful live extraction on the exact authorized candidate origin, merged-release CI, deployment/promotion/rollback, first-time user observations, actual domain-expert feedback, final human authorship and archival distribution review still require external evidence. Historical underperformance, unsupported macro/causal interpretations, and cross-Node last-digit differences remain explicit limitations. None is repaired by relabeling results.

## Recommendations

1. Fix the unsupported financial authoring affordance in one bounded wave, then obtain scoped re-review. Retain every external gate in the readiness report.
2. Keep the Vitest upgrade as a separately verified task; do not broaden this review to untested environments or scientific claims.
3. Optionally change the release document's “planned security patch” to completed-patch wording for consistency.

One aggregate fix wave is requested, limited to the financial authoring affordance and focused regression/browser checks. The optional future-tense cleanup may be included. No scientific or financial-model change is needed.

## Confidence Assessment

**High confidence in the important finding and the task-6 acceptance.** Evidence comprises exact-range source inspection, prior independent scoped reviews, fresh source/authority/immutability checks, recorded browser artifacts, and focused independent execution. Confidence does not extend to the unresolved external gates.

Independent financial probe: `/private/tmp/beta-final-integration-probe.ts`, executed with `/private/tmp/history-node22/package/bin/node --import tsx /private/tmp/beta-final-integration-probe.ts`: 24 exact handoffs passed; an added probe confirmed zero financial authoring targets and the failed default Apply. Independent selector check: `node node_modules/vitest/vitest.mjs run components/lab/PolicyPanel.lifecycle.test.tsx`: ten tests passed. No full-suite duplication, production edits, commits or pushes were performed.

## Scoped final re-review — e6a02a8..84075ba

**ACCEPTED. The single important financial-authoring finding is resolved. This scoped verdict supersedes the initial request for a fix; task 6 remains accepted.** No further critical or important defect was found. All previously stated research-beta and external-delivery limitations remain in force.

The exact eight-file patch and supplied review package were compared programmatically. Changes are limited to the authoring affordance, CTA label, focused tests, implementation report and the requested release-document tense correction. No financial equation, hook, observation, model identity, runtime, qualification source or frozen result changed.

`AddVariableForm` now replaces creation controls when there are zero eligible targets with an explicit unavailable message and concrete export/edit/import instructions. The referenced controls actually exist in `ModelFilePanel` and `ModelImportPanel`. The guidance correctly describes an edited model as experimental and requiring its own tests/review. The enabled-model creation branch remains unchanged, while existing overlay removal remains outside the conditional and works in either branch. The published CTA now says “Inspect extension options” and retains the same exact scenario-A authoring URL.

Fresh independent Node 22.23.2 verification: `node node_modules/vitest/vitest.mjs run components/lab/AddVariableForm.test.tsx` passed all four tests, including a real engine-valid overlay and removal callbacks for both model types. The worker's broader 39-test run and final successful typecheck were inspected as reported evidence, not rerun wholesale.

Inspected root browser evidence closes the actual user-flow checks:

- `evidence/beta-delivery-2026-09-15/financial-authoring-fix.json` shows the correct author section focused, the explicit limitation and route, zero form inputs and zero Apply buttons. Root also followed Equations and files and downloaded `/private/tmp/beta-financial-authoring-export.json`; independent parsing confirms model `reported-allocation-apple-fy2025` and the model/overlay export envelope.
- `evidence/beta-delivery-2026-09-15/supported-authoring-fix.json` records Income and wellbeing accepting the unchanged `newAccess` multiply form, showing overlay `lab-newAccess`, and removing it using the native removal button. Before/after snapshots contain no alerts. The displayed `lab-newAccess1 effect` is concatenated overlay id plus effect count, not a different overlay id.
- The retained final Node 22 build log `/private/tmp/beta-authoring-final-build.log` ends with a successful production build. The root reports exit 0. This targeted fix did not require a repeated full numerical suite or renewed source qualification.

No additional fix wave is requested. Proceed with the bounded handoff and retain the unresolved live-provider, merged-release CI, deployment, user/expert feedback and archive gates.
