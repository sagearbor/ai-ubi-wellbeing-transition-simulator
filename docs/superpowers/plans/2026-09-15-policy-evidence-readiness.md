# Policy evidence and evaluation readiness

User authorization: verify live AI extraction in the candidate, repair the clean-machine check, add an evaluation whose scoring years are excluded from fitting, and make policy evidence boundaries clear. Separate follow-up branch preserves PR20 at a064c2c. No merge/deploy yet; publish reviewable GitHub work when complete.

Use subagent-driven development: one implementation worker at a time, independent task reviews, concrete scoped fixes, one final broad review plus one bounded fixwave. Root does not implement production code. Existing published observations, model targets and fitted defaults must not be silently re-pinned. The user already authorized work; do not ask for another approval menu.

## Tasks

1. Build the preregistered historical data/fit/prediction/scoring pipeline through the existing level engine, with only an explicit frozen-coefficient seam. Protocol is committed before fitting or inspecting scores. Read forecast-preflight.md. Own new evaluation data/scripts/tests and minimal engine seam; preserve exact default behavior. Historical reconstruction report unchanged; metadata/source identities updated honestly. Do not declare existing default qualification current after source changes.
2. Refresh source-bound qualification for the unchanged default arithmetic and validate engine/source compatibility. Run and retain new evidence for new source identity; do not modify targets, equations or scores to pass. Own qualification tooling/evidence metadata and verification docs only as required by the actual dependency change.
3. Expose temporal holdout and policy evidence in the interface. Primary history navigation distinguishes reconstruction from the frozen temporal holdout. Policy results distinguish policy settings, reported inputs, assumption-dependent mechanisms and unsupported effects; no automatic causal-evidence label from an LLM or a source URL. Keep advanced model/variable/policy tools discoverable and all existing state/share owners intact. Own UI + a tested dependency/evidence presentation helper if needed, without changing scientific equations.
4. Verify the live AI workflow in the exact candidate environment, including supported funding, unsupported effect, invalid/conflicting mapping and refusal/recovery. Credentials remain local/gitignored, never in logs or GitHub. Do not weaken review or evidence gates to make a demo run. Root performs actual browser tests and records versions, synthetic source text and replay evidence. Publish the final branch/draft PR with exact SHA, CI and deployment/readiness limits.

## Acceptance

- Training 2015–2018, origin2018, tests2019–2025; historical2015 backgrounds with outcome-independent governance transform; no current numeric country snapshots.
- Physically separate train/origin/frozen fit/prediction/test-score artifacts and code interfaces. Fit/predict do not read test outcomes. Every excluded country/year visible, no endpoint-selected cohort.
- Existing full anchored level engine, AI/UBIoff; only coefficients refit on train. Existing defaults and published targets unchanged; temporal holdout claims limited to out-of-fit retrospective performance with revised vintages.
- Performance can be poor. CI gates integrity/reproducibility, not beating persistence. No tune-until-green cycle.
- New source identity receives fresh appropriate qualification evidence; old review is not reused without verification.
- AI can prepare a supported draft and run an honestly labeled comparison; unsupported mechanisms stay explicit, not converted into evidence by the model.
- Final tests, desktop/mobile browser review, source/evidence review, exact GitHub comparison and a deployment checklist based on tested facts.
